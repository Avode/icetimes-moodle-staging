# Student Dashboard Block (wired personal info)
Install to `moodle/blocks/student_dashboard`, purge caches, add the block.
Personal section pulls from core user, custom profile fields ('program','orgunit'), and cohorts.
